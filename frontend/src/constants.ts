export const MAX_LOG = 500;

export const FILE_SIZE_HIST_CHART_PX = 52;

/** Initial expand depth: layers 0–2 expanded; deeper folders start collapsed. */
export const TREE_INITIAL_EXPAND_MAX_DEPTH = 2;
